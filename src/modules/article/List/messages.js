import { defineMessages } from 'react-intl';

const basePath = 'module.character.content';

const messages = defineMessages({
    listPlaceholder: {
        id: `${basePath}.listPlaceholder`,
        defaultMessage: 'List placeholder',
    },
});

export default messages;
