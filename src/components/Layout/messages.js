import { defineMessages } from 'react-intl';

const basePath = 'component.layout';

const messages = defineMessages({
    title: {
        id: `${basePath}.title`,
        defaultMessage: 'The One Forge',
    },
});

export default messages;
