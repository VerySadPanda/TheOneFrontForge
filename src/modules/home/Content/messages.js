import { defineMessages } from 'react-intl';

const basePath = 'module.home.content';

const messages = defineMessages({
    description: {
        id: `${basePath}.description`,
        defaultMessage: 'Why is my panda so very sad?',
    },
});

export default messages;
